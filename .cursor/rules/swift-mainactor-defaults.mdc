---
description: 避免 MainActor 类型在默认参数里 init（SWIFT_DEFAULT_ACTOR_ISOLATION）
globs: DreamWeaver/**/*.swift
alwaysApply: false
---

# MainActor 与默认参数

工程开启了 `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`。多数 App/Service 类型的 `init()` 是 main actor-isolated。

## 禁止

在参数默认值里直接构造 MainActor 类型（默认值表达式在 **nonisolated** 上下文求值）：

```swift
// ❌ 会报：Call to main actor-isolated initializer 'init()' in a synchronous nonisolated context
init(fallback: LocalFoo = LocalFoo()) { ... }
```

同类雷区：`LocalContentService()`、`LocalUserLibraryService()`、`LocalSeedPipelineService()`、`LocalPlaybackService()`、`LocalAnalyticsService()`、`DemoPersistenceStore()` 等作为默认实参。

## 正确写法

在 `@MainActor` 的 `init` **函数体**里构造，默认参数用 `nil`：

```swift
// ✅
init(fallback: LocalFoo? = nil) {
    self.fallback = fallback ?? LocalFoo()
}
```

或由已在 MainActor 上的调用方显式传入（如 `AppState` convenience init）。

## 检查

改 Services 的 `init` 后，确认没有 `= SomeService()` / `= SomeStore()` 形式的默认参数；静态 `.shared` 若也报隔离错误，同样改为体内赋值或调用方传入。
