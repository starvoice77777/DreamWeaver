# 真机远程联调：已实现能力与测试清单

> 分支：`integration/frontend-backend`  
> 适用：Windows 跑 API + Mac/Xcode 真机安装  
> 更新：2026-08-04（已补「创建」场景创作）

---

## 一、当前已实现能力（验收范围）

### 1. 客户端运行模式

| 能力 | 说明 |
|------|------|
| 本地演示 | Bundle + 本地 Service，可离线演示 |
| 远程 API | 冷启动生效；设置里切换后需完全退出重开 |
| 远程基址 | `dw.apiBaseURL`；真机默认局域网 IP，可在设置中改并保存 |
| 局域网 HTTP | `NSAllowsLocalNetworking` 已开 |

### 2. 内容与播放

| 能力 | 说明 |
|------|------|
| 官方场景目录 | 远程 `GET /v1/scenes` + 详情；本地 Mock 兜底 |
| 时间线编排 | `GET /v1/scenes/{id}/timeline`；客户端调度分层进场 |
| 本地多轨播放 | `LocalPlaybackService` + AVAudioEngine；圆盘改混音实时同步 |
| 场景切换 | 卡片进「此刻」、上下滑切场景、封面/时间线预取 |
| 官方混音预设 | 服务端 2 条：洗头轻声、细雨慢听；「此刻」底部 **我的 \| 预设** 可选 |
| 定时 | 自动 / 10 / 30 / 60 / 一直；演示加速（演示控制开启时） |

### 3. 创建 Tab（场景创作）

| 能力 | 说明 |
|------|------|
| 一级入口 | 底部 Tab「创建」→ `CreateHubView` |
| 从空白开始 | 进入空间编辑器（空白 seed） |
| 从已有场景创建 | 选官方/当前场景列表为底稿，再进编辑器 |
| 空间画布 | 拖声源改**当前选中关键帧**位置；可过圆心 |
| 材料库 | 底部材料 chip 增轨（编辑器内 catalog） |
| 时间轴 | 关键帧点选 / 游标插入；音频轨起止与移动；文字 cue |
| 预览播放 | 编辑器内 scrub / play（插值预览） |
| 保存 | **本机草稿**可在「创建 → 我的草稿」找回；远程登录后同步云端私人场景（`draft_composition`） |
| 云端列表 | 登录后额外显示「云端私人场景」（本机未镜像的项） |

> 勾选保存为**草稿**（本机必落盘）；登录远程时写入 `draft_composition`，不会自动 `POST .../save` 发布正式版。契约见 `docs/scene-composition-contract.md`。

### 4. 账号与云端状态

| 能力 | 说明 |
|------|------|
| 开发登录 | `dev:demo-user`（联调主路径） |
| Sign in with Apple | 真机可用（需 Bundle `zhimeng.DreamWeaver.df` + Capability） |
| 会话保持 | Keychain token；冷启动可保持登录 |
| 收藏官方场景 | 登录后 patch scene-state；home 恢复 |
| 用户设置同步 | 自动播放等 ↔ `PUT/GET /v1/users/me/settings` |
| 云端保存混音 | 设置页「云端保存当前混音」（显式保存；圆盘混音路径） |
| Home | 收藏 / 最近 / 私人场景摘要 |

### 5. 声音库与种子（远程需登录）

| 能力 | 说明 |
|------|------|
| 资产列表 | 登录后拉 `/v1/library/assets` |
| 上传 / 删除 | 需登录；未登录有明确提示 |
| 声音种子流程 | 质检 → 授权 → 任务 → 保存（Stub；产品路径已废弃，入口宜隐藏，遗留可测） |

### 6. 工程 / 联调配置

| 项 | 当前值 |
|----|--------|
| Team | `DF548S4JJY` |
| Bundle ID | `zhimeng.DreamWeaver.df` |
| `DW_APPLE_CLIENT_ID` | 与 Bundle 一致（见 `server/.env.example`） |
| 默认真机基址示例 | `http://192.168.120.62:8000`（换网需改） |

### 7. 明确不在本轮范围 / 已知占位

- 人声短句 `voice_phrase_mom` 可能仍为静音占位  
- 远程官方预设目前仅洗头、雨檐各 1 条；其它场景只有「我的」  
- **创建编辑器正式发布**（`saved_composition`）尚无独立 UI；当前勾选为草稿  
- 离线写队列完整回放尚未作为联调必测项  
- 勿恢复已删除的 `SceneDetailSheet` / `ScenePreviewView`

---

## 二、开测前检查

| # | 操作 | 期望 | 结果 |
|---|------|------|------|
| 0.1 | 电脑 `http://127.0.0.1:8000/health`、`/ready` | ok / ready | [ ] |
| 0.2 | 手机 Safari 打开 `http://<局域网IP>:8000/health` | 可见 JSON | [ ] |
| 0.3 | 手机与电脑同 Wi‑Fi，关 VPN | 可达 | [ ] |
| 0.4 | App 设置：基址正确；内容数据源 = 远程 API | 文案「当前进程：远程 API」 | [ ] |
| 0.5 | 若仍显示本地演示 | 完全划掉 App 再开 | [ ] |

记录：基址 `________________`　设备 / iOS `________________`

---

## 三、测试操作清单

### A. 冷启动与游客态

| # | 操作 | 期望 | 结果 |
|---|------|------|------|
| A1 | 冷启动进「此刻」 | 无闪退；场景列表非空 | [ ] |
| A2 | 未登录播放官方场景 | 能出声 | [ ] |
| A3 | 设置「远程会话」 | 游客（可开发登录） | [ ] |
| A4 | 未登录点上传 / 创建种子 | 提示需登录 | [ ] |

### B. 开发登录

| # | 操作 | 期望 | 结果 |
|---|------|------|------|
| B1 | 开发登录（dev:demo-user） | 「已登录：…」 | [ ] |
| B2 | 完全退出再开 | 仍保持已登录 | [ ] |
| B3 | 退出远程登录后再冷启动 | 回到游客 | [ ] |

### C. 场景、时间线、播放

| # | 操作 | 期望 | 结果 |
|---|------|------|------|
| C1 | 「洗头陪伴」 | 水流 / 吹风机 / 底噪等可闻（人声可静音占位） | [ ] |
| C2 | 「檐下听雨」 | 雨、风等分层；时间线 cue 陆续进场 | [ ] |
| C3 | 场景卡片进「此刻」 | 无旧预览 Sheet | [ ] |
| C4 | 上下滑切场景 | 切场与播放重载正常 | [ ] |
| C5 | 暂停 / 播放 | 与出声一致 | [ ] |

### D. 官方混音预设（本轮重点）

| # | 操作 | 期望 | 结果 |
|---|------|------|------|
| D1 | 洗头场景看底部控件上方 | 出现 **我的 \| 洗头轻声** | [ ] |
| D2 | 点「洗头轻声」 | 布局切换为预设；不可拖改圆盘 | [ ] |
| D3 | 点「我的」 | 恢复可编辑个人混音 | [ ] |
| D4 | 切到檐下听雨 | 出现 **我的 \| 细雨慢听** | [ ] |
| D5 | 点「细雨慢听」再回「我的」 | 试听 ↔ 可编辑切换正确 | [ ] |
| D6 | 其它无官方预设的场景 | 仅「我的」（可接受） | [ ] |

### E. 圆盘混音

| # | 操作 | 期望 | 结果 |
|---|------|------|------|
| E1 | 「我的」下拖声源 | 空间感变化；其它轨不整段死静音 | [ ] |
| E2 | 调音量 / 开关 / 增删 | 播放同步；不崩溃 | [ ] |
| E3 | 调色盘与闲置收起 | 约 5s 逻辑正常 | [ ] |

### F. 创建 Tab（场景创作 — 本轮必测 UI）

| # | 操作 | 期望 | 结果 |
|---|------|------|------|
| F1 | 底部点「创建」 | 进入创建 Hub，见「从空白开始」「从已有场景创建」 | [ ] |
| F2 | 「从空白开始」 | 全屏进空间编辑器；可返回创建 | [ ] |
| F3 | 「从已有场景创建」 | 弹出场景列表；选洗头/雨檐等可进编辑器且带底稿声源 | [ ] |
| F4 | 材料库加轨 | 画布出现新声源；可再次点选已有材料 | [ ] |
| F5 | 拖画布声源 | 改当前关键帧位置；不崩溃 | [ ] |
| F6 | 时间轴：加关键帧 / 拖关键帧时间 / 删关键帧 | UI 与状态一致 | [ ] |
| F7 | 调整音频轨起止或移动 | 边界合理；可完成编辑 | [ ] |
| F8 | 添加 / 编辑文字 cue | 列表更新；可删除 | [ ] |
| F9 | 播放 / 暂停 / scrub | 游标移动；插值预览不卡死 | [ ] |
| F10 | 填写场景名后点保存（勾） | toast 提示已保存；返回创建页可见「我的草稿」 | [ ] |
| F11 | 未填名称点保存 | toast 提示先填写场景名称 | [ ] |
| F12 | 从「我的草稿」再点进同一草稿 | 名称 / 声源 / 关键帧仍在 | [ ] |
| F13 | 长按草稿删除（若支持） | 列表移除 | [ ] |
| F14 | 登录远程后保存带声源的草稿 | 本机列表显示「已同步云端」；或见「云端私人场景」 | [ ] |
| F15 | 重置/清空（若入口可见） | 内容清空有反馈 | [ ] |

> F10–F14：保存为草稿；正式发布（`POST .../save`）仍待产品入口。

### G. 登录后云端：收藏与设置（冷启动必测）

| # | 操作 | 期望 | 结果 |
|---|------|------|------|
| G1 | 收藏场景 | 星标变化 | [ ] |
| G2 | 关「自动播放」等，等约 1s 或切后台 | 无「设置同步失败」 | [ ] |
| G3 | 完全退出再开 | 收藏仍在；自动播放等保持 | [ ] |
| G4 | 「云端保存当前混音」 | 有成功类服务消息 | [ ] |

### H. 定时

| # | 操作 | 期望 | 结果 |
|---|------|------|------|
| H1 | 自动 / 10 / 30 / 60 / 一直 | 切换无崩溃 | [ ] |
| H2 | 演示加速 | 约数十秒内渐隐停播 | [ ] |

### I. 声音库 / 种子（可标延后）

| # | 操作 | 期望 | 结果 |
|---|------|------|------|
| I1 | 登录后声音库刷新 | 能拉列表（可为空） | [ ] |
| I2 | 未登录上传 | 明确拦截 | [ ] |
| I3 | 上传或种子全流程 | 状态文案正确或明确失败 | [ ] 延后 |

### J. Apple 登录（次要）

| # | 操作 | 期望 | 结果 |
|---|------|------|------|
| J1 | 真机 Apple 登录 | 可完成或可读错误 | [ ] 延后 |
| J2 | 失败时 | 可用开发登录兜底 | [ ] |

### K. 异常与模式切换

| # | 操作 | 期望 | 结果 |
|---|------|------|------|
| K1 | 断 API / Wi‑Fi 后刷场景 | 可读错误，不白屏崩溃 | [ ] |
| K2 | 恢复网络 | 可再加载 | [ ] |
| K3 | 切本地演示 → 完全退出重开 | 当前进程：本地演示 | [ ] |
| K4 | 再切远程 → 完全退出重开 | 远程再次可用 | [ ] |
| K5 | 远程/本地下创建 Tab 仍可打开编辑器 | 创作 UI 不依赖登录（当前） | [ ] |

---

## 四、最小通过标准（今日联调）

1. [ ] 远程冷启动能播官方场景  
2. [ ] 开发登录成功且冷启动保持会话  
3. [ ] 洗头 / 雨檐能看到并切换对应官方预设  
4. [ ] 圆盘「我的」下改混音实时出声  
5. [ ] 收藏或设置至少一项冷启动后仍在  
6. [ ] **创建**：空白进入 + 保存后能在「我的草稿」找回并再编辑  
7. [ ] 未登录上传被拦截  

---

## 五、失败记录模板

```
日期：
分支：integration/frontend-backend
基址：
设备 / iOS：

通过项：A… / B… / F… / …
失败项：
  - 编号：
  - 操作：
  - 现象 / lastServiceMessage：
  - 是否可复现：
```

---

## 六、队友同步

```bash
git fetch origin
git switch integration/frontend-backend
git pull origin integration/frontend-backend
```

本轮相关共享文件（危险区）：`AppState.swift`、`SoundMixCircleEditor.swift`、`Models/SoundAsset.swift`。  
创建相关：`Views/Create/**`、`RootTabView.swift`。改同文件前请先沟通。
